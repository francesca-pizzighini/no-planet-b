function SideChart({ title, description }) {
  return (
    <>
        <div className="chart-graph" ></div>

        <h2>{title}</h2>

        <p>{description}</p>
    </>
  )
}

export default SideChart
